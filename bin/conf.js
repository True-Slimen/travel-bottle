/**
 * All keys must have the same name as the environment variable
 * All environments variables are tested before starting app
 * Only UPPERCASE variables are considered as environment variables and tested
 */
module.exports = {
	port: 8000,
};
